# M0 H20 top5×h5 早停 Implementation Plan

> **For agentic workers:** Execute inline. User waived further approval gates.

**Goal:** Retrain M0 H20 (5 seeds) with early stop = top5×h5 `net_ann_excess` on all-A eval days.

**Architecture:** Optional `es_metric` on `RegimeSingleLGBMModel`; feval reuses `daily_head_panel` + `summarize_head_series`. New valid cache, new sessions.

**Tech Stack:** LightGBM, Qlib, existing Phase M v1 eval.

## Global Constraints

- Do not overwrite `regimeadaptfast_m0h20_s*` or `valid_frame_70.pkl`
- Default `es_metric=daily_rank_ic` stays unchanged
- No baseline promotion; no EXPERIMENT_STANDARD edit
- Python: `/opt/anaconda3/envs/qlib/bin/python`
- No heredoc/stdin for Qlib parallel fetch

---

### Task 1: feval + model switch

**Files:**
- Modify: `backtest/models/regime_adapt.py`
- Modify: `backtest/scripts/train_regime_arm.py`
- Test: `tests/backtest/test_regime_adapt.py`

- [x] Write failing tests for `top5_h5_net_ann` and default metric
- [x] Implement helper + `es_metric` + `--es-metric`
- [x] New valid panel builder/cache `valid_frame_t5h5es.pkl`
- [x] Train 5 seeds, eval, registry, HTML
